# 🚀 Next.js 博客个性化修改清单

> Fork 自 ObjectX 的博客项目，以下是你需要修改的所有内容。

---

## 📋 快速概览

| 优先级 | 类别 | 预计时间 |
|--------|------|----------|
| 🔴 必须 | 环境配置 | 10 分钟 |
| 🔴 必须 | 个人信息 | 30 分钟 |
| 🟡 重要 | SEO 与品牌 | 20 分钟 |
| 🟢 可选 | 功能模块清理 | 按需 |

---

## 🔴 1. 环境配置（必须）

### 创建 `.env.local` 文件

在项目根目录创建 `.env.local`：

```env
# ========== 基础配置 ==========
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ========== 数据库（必须）==========
MONGODB_URI=mongodb://localhost:27017/your_blog_db

# ========== 认证（必须）==========
JWT_SECRET=替换为一个随机的安全密钥
JWT_EXPIRES_IN=7d

# ========== 阿里云 OSS（可选，用于图片上传）==========
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=你的AccessKeyId
OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
OSS_BUCKET=你的bucket名称

# ========== Google Analytics（可选）==========
# 如果不需要可以不配置
```

---

## 🔴 2. 个人信息修改（必须）

### 2.1 社交链接
**文件**: `src/config/social-links.ts`

```typescript
// 需要修改的内容：
export const socialLinks = [
  {
    "name": "博客",
    "icon": "🌐",
    "url": "https://你的博客地址.com",  // ← 修改
    "bgColor": "#e8f5e9"
  },
  {
    "name": "掘金",
    "icon": "📍", 
    "url": "https://juejin.cn/user/你的ID",  // ← 修改
    "bgColor": "#ffebee"
  },
  {
    "name": "Github",
    "icon": "👨‍💻",
    "url": "https://github.com/你的用户名",  // ← 修改
    "bgColor": "#f3e5f5"
  },
  // ... 其他链接按需修改或删除
];
```

---

### 2.2 工作经历
**文件**: `src/config/work-experience.ts`

```typescript
// 替换为你的工作经历
export const workExperiences: WorkExperience[] = [
  {
    "company": "你的公司名称",
    "companyUrl": "https://公司网站.com/",
    "position": "你的职位",
    "description": "工作描述",
    "startDate": "2022-07-01",
    "endDate": null  // null 表示至今
  },
  // 添加更多工作经历...
];
```

---

### 2.3 项目展示
**文件**: `src/config/projects.ts`

```typescript
// 替换为你的项目
export const projectData: ProjectCategory[] = [
  {
    "name": "你的项目分类",
    "description": "分类描述",
    "projects": [
      {
        "title": "项目名称",
        "description": "项目描述",
        "github": "https://github.com/你的用户名/项目名",
        "url": "https://项目演示地址.com",
        "tags": ["Next.js", "React", "TypeScript"],
        "status": "completed"  // completed | in-progress | planned
      }
    ]
  }
];
```

---

### 2.4 友情链接
**文件**: `src/config/friends.ts`

```typescript
// 替换为你的友链
export const friends: Friend[] = [
  {
    avatar: "头像URL",
    name: "朋友名称",
    title: "标题/职位",
    description: "描述",
    link: "https://朋友的网站.com/",
    position: "职位",
    location: "位置",
    isApproved: true  // 是否显示
  }
];
```

---

## 🟡 3. SEO 与品牌修改（重要）

### 3.1 网站元信息
**文件**: `src/app/layout.tsx`

需要修改的关键位置：

```typescript
// 第 22-28 行：JSON-LD 结构化数据
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: '你的博客名称',  // ← 修改
  url: 'https://你的域名.com',  // ← 修改
  logo: 'https://你的域名.com/logo.png',  // ← 修改
  sameAs: [
    'https://github.com/你的用户名',  // ← 修改
  ]
};

// 第 31-40 行：默认 Meta 信息
return {
  title: "你的博客标题",  // ← 修改
  description: "你的博客描述",  // ← 修改
  openGraph: {
    title: '你的博客名称',  // ← 修改
    siteName: "你的博客名称",  // ← 修改
    // ...
  },
};

// 第 52-69 行：文章 JSON-LD（建议删除或修改）
const jsonLdData = {
  // 这是示例数据，建议删除
};
```

---

### 3.2 网站图标
**文件**: `src/app/favicon.ico`

替换为你自己的网站图标（推荐 32x32 或 64x64 像素）

---

### 3.3 公共资源
**目录**: `public/`

检查并替换：
- [ ] Logo 图片
- [ ] OG 分享图片
- [ ] 其他品牌相关资源

---

## 🟢 4. 功能模块清理（可选）

根据需要保留或删除以下页面模块：

| 模块 | 目录 | 建议 |
|------|------|------|
| 📝 文章 | `src/app/articles/` | ✅ 保留（核心功能） |
| 📷 相册 | `src/app/album/` | 按需保留 |
| 🔖 书签 | `src/app/bookmarks/` | 按需保留 |
| 💼 项目 | `src/app/projects/` | ✅ 推荐保留 |
| ⏳ 时间线 | `src/app/timeline/` | 按需保留 |
| 👥 友链 | `src/app/friends/` | 按需保留 |
| 🏋️ 健身 | `src/app/fitness/` | ⚠️ 可删除（演示功能） |
| ✈️ 旅行 | `src/app/travel/` | ⚠️ 可删除（演示功能） |
| 💡 灵感 | `src/app/inspirations/` | 按需保留 |
| ✅ 待办 | `src/app/todos/` | 按需保留 |
| 🔧 管理后台 | `src/app/admin/` | ✅ 保留（管理内容） |

---

## 🗂️ 5. 其他配置文件

| 文件 | 用途 | 是否需要修改 |
|------|------|--------------|
| `src/config/stacks.ts` | 技术栈展示 | 按需修改 |
| `src/config/timelines.ts` | 时间线配置 | 按需修改 |
| `src/config/photos.ts` | 相册配置 | 按需修改 |
| `src/config/bookmarks.ts` | 书签配置 | 按需修改 |
| `src/config/workspace-items.ts` | 工作区配置 | 按需修改 |

---

## ✅ 完成检查清单

- [ ] 创建 `.env.local` 并配置数据库连接
- [ ] 修改 `social-links.ts` 社交链接
- [ ] 修改 `work-experience.ts` 工作经历
- [ ] 修改 `projects.ts` 项目展示
- [ ] 修改 `friends.ts` 友情链接
- [ ] 修改 `layout.tsx` SEO 信息
- [ ] 替换 `favicon.ico`
- [ ] 清理不需要的功能模块
- [ ] 运行 `pnpm dev` 测试

---

## 🚀 启动命令

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 访问
open http://localhost:3000
```

---

> 💡 **提示**: 建议先完成 🔴 必须项，确保项目可以正常运行后，再逐步完成其他修改。
